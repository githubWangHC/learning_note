<?xml version="1.0" encoding="UTF-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@id/timer_narrow_layout"
    android:layout_width="@dimen/timer_standard_width"
    android:layout_height="102dp"
    android:background="@mipmap/timer_narrow_background"
    android:orientation="horizontal">

    <TextView
        android:id="@id/timer_narrow_minute_textview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:paddingBottom="2.0dip"
        android:text="@string/timer_end"
        android:textColor="@color/white"
        android:textSize="65.0sp"
        android:textStyle="normal" />

</RelativeLayout>

